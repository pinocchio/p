NEW_TEST(variable_lookup)
    // TODO complete test
    AST_Variable var = new_AST_Variable(L"test");
    var->index         = 0;
    var->key           = (Object)new_Type_SmallInt(10);
    
    Env = (Object)new_Runtime_Env_Sized((Object)current_env(), var->key, 1);
    Env = (Object)new_Runtime_Env_Sized((Object)current_env(), Nil, 0);
}

/* ======================================================================== */

TEST_SUITE(Runtime_Env,
{
    RUN_TEST(variable_lookup);
})