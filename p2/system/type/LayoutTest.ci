
#define TYPE_LAYOUT_TEST_SETUP \
    Type_Layout layout = new_Type_Layout(); \
    Object result;

/* ========================================================================= */

void test_TYPE_LAYOUT_example() {
 #error Fill the tests
}

/* ========================================================================= */

TEST_SUITE(Type_Layout,
{
   RUN_TEST(TYPE_LAYOUT_example);
})

