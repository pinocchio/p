
void test_IO_File_stdout()
{
    Object result;
    result = Eval_AST_Send1((Object)StandardOut, SMB_writeAll_, (Object)new_Type_String(L"Hello World\n"));
    //result = Eval_AST_Send0((Object)StandardOut, SMB_readAll);
    //result = Eval_AST_Send0((Object)StandardIn, SMB_readAll);
}

/* ======================================================================== */

TEST_SUITE(IO_File,
{
    RUN_TEST(IO_File_stdout);
})