pre_init_Type_Nil();
pre_init_Type_Class();
pre_init_Type_Object();

// Basic objects required for Layout, Slot, Slot names
pre_init_Number_SmallInt();
pre_init_Number_Float();
// Layout needs to be initialized before the symbol's layout is created
pre_init_Type_Layout();
pre_init_Type_Symbol();
pre_init_Slot_Slot();

// Symbol table for Slot names
pre_init_Collection_Array();
pre_init_Collection_DictBucket();
pre_init_Collection_Dictionary();
init_SymbolTable();
