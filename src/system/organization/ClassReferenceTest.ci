
#define ORGANIZATION_CLASSREFERENCE_TEST_SETUP \
    Organization_ClassReference classreference = new_Organization_ClassReference(); \
    Object result;

/* ========================================================================= */

void test_ORGANIZATION_CLASSREFERENCE_example() {
 #error Fill the tests
}

/* ========================================================================= */

TEST_SUITE(Organization_ClassReference,
{
   RUN_TEST(ORGANIZATION_CLASSREFERENCE_example);
})

