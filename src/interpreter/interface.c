#include <pinocchio.h>

/* ======================================================================= */

void send_message(Thread thread, Object receiver,
                  Symbol selector, uns_int argc, ...)
{
}

void send_message_with(Thread thread, Object receiver,
                       Symbol selector, Array arguments)
{
}
